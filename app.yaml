runtime: python311

env_variables:
  DJANGO_SETTINGS_MODULE: 'settings_production'
  DJANGO_SECRET_KEY: 'gcp-django-secret-key-fingerprint-evm-2024'
  DEBUG: 'False'
  FIREBASE_CREDENTIALS_PATH: 'firebase-credentials.json'
  FIREBASE_DATABASE_URL: 'https://fingerprint-evm-default-rtdb.asia-southeast1.firebasedatabase.app/'

handlers:
- url: /static
  static_dir: static/
- url: /.*
  script: auto

automatic_scaling:
  min_instances: 1
  max_instances: 10
  target_cpu_utilization: 0.6